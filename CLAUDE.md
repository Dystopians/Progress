@AGENTS.md

## 仅对 Claude 的补充

- 本工程的协作手册是上面导入的 `AGENTS.md`（Codex 也读同一份）。规则、命令或目录变了，改 `AGENTS.md`，不要把内容写在这里另起一份。
- Bash 工具里用 heredoc 写含反斜杠的补丁脚本会被改写：先用 Write 工具把脚本写成文件再运行；替换前断言锚点恰好出现一次。
- 子代理少用：只给边界清楚、可独立验收的任务，并在说明里写明可改与不可改的文件；根因调试在主会话做。
