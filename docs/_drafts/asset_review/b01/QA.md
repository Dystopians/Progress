# B01 资源检查记录

2026-09-22 · Codex · 制作与自查完成，待用户初审，未接入游戏。

## 资产检查

- PNG 9/9，全部 1254 × 1254，共 10,932,505 字节（10.43 MiB）。
- 完整提示词 9 份，修正提示词 1 份。SHA-256、生成源文件名、家族、时代、版本见 manifest.json。
- System.Drawing 只读扫描全部像素：9/9 有完全透明背景与不透明主体；8/9 外圈 alpha 全零。
- 蒸汽工厂例外：x=0、y=629–634 共 6 像素 alpha=1/255。主体包围盒在图内，保留此近透明边缘残留并公开，不假称全零。
- 复查命令：`powershell -NoProfile -ExecutionPolicy Bypass -File docs/_drafts/asset_review/b01/inspect_assets.ps1`。只读图片、更新元数据，不编辑像素。
- 已实际查看深底、纸面、灰度截图（overview.jpg / review_paper.jpg / review_gray.jpg）；完整审核页 27/27 图像元素加载成功。
- 128 px 保留主要轮廓；64 px 丢失机械及门窗细节，教育家族尤其依赖名称标签。灰度可分主要屋顶与形体，不能用图像代替玩法数据。
- 轻微材质和体积明暗偏插画感，是待初审方向，不默认为修改 docs/20 风格合同。
- 现代工厂黄黑条由 imagegen 修正。未用脚本抠图、调色、裁切或重绘；截图来自真实浏览器，JPEG 按真实编码保存。

## 工程验证

- Python 分层闸门：无违规。
- Git Bash 因本机管道权限限制未启动，改由 PowerShell 直接执行同一 Godot 引擎的 parse_check.gd。
- 解析：138/138，失败 0，进程退出 0；9 张 PNG 生成导入记录。
- 审核目录加 .gdignore，网页、截图与未采用版本不作为游戏资源导入。
- 环境提示：根证书库读取失败、用户目录日志／编辑器设置写入失败；解析退出仍有既有 ObjectDB 32 个、资源 28 个未释放提示，不能称为零错误输出。
- 首次审核截图为 JPEG，临时用了 PNG 后缀，导致导入报错；已改按真实编码保存 .jpg，删除两份错误命名的自生成截图，并隔离审核目录。
- 本轮未改游戏代码或 content，未重复跑上一轮 540 项全量测试；未导出、提交、推送。资源自查不能替代后续接入后的截图、资源包与运行验证。

## 审核方式

overview.html 展示三家族 × 三时代；index.html 检查 128／64 px、底色和灰度。可直接打开本地 HTML。ZIP 解压后保留目录层级；包含 PNG、提示词、元数据、审核网页与三张截图，未采用 v1 不进包。

打包检查：30 个文件，72 处本地链接存在，ZIP CRC 检查通过，包内 9 张 PNG 的 SHA-256 与 manifest 一致。打包脚本为本目录 package_review.py；包中不含该脚本、Godot 导入缓存或未采用 v1。
